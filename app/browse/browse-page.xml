<Page
    class="page"
    navigatingTo="onNavigatingTo"  
    xmlns="http://schemas.nativescript.org/tns.xsd">

    <!-- <ActionBar class="action-bar">
        <Label class="action-bar-title" text="Browse"></Label>
    </ActionBar> -->

    <FlexboxLayout flexDirection="column" alignItems="center">
        <!-- <Label class="page-icon fa" text="&#xf1ea;"></Label> -->
        <!-- <Label class="page-placeholder" text="Browse page content goes here"></Label> -->
        <!-- https://docs.nativescript.org/ui/styling#application-wide-css these can't flex for some reason -->
        <TextView
            id="input"
            height="45%"
            width="100%"
            editable="true"
            loaded="{{ onComponentLoaded }}"
            hint="Enter Javascript code"
            text='{{ inputValue }}'
            secure="false"
            keyboardType="datetime"
            returnKeyType="done"
            autocapitalizationType="none"
            returnPress="{{ onReturnPress }}"
            autocorrect="false"
            focus="{{ onInputFocus }}" 
            blur="{{ onInputBlur }}" 
            class="input input-border"
        />
        <TextField
            id="ownProps"
            height="5%"
            width="100%"
            editable="false"
            loaded="{{ onComponentLoaded }}"
            hint="(Own properties)"
            text='{{ ownPropsValue }}'
            class="input input-border"
            backgroundColor="rgb(220,240,240)"
            style="padding-left: 15px;"
        />
        <TextField
            id="inheritedProps"
            height="5%"
            width="100%"
            editable="false"
            loaded="{{ onComponentLoaded }}"
            hint="(Inherited properties)"
            text='{{ inheritedPropsValue }}'
            class="input input-border"
            backgroundColor="rgb(220,220,240)"
            style="padding-left: 15px;"
        />
        <ScrollView height="35%">
            <TextView
                id="output"
                height="100%"
                width="100%"
                editable="false"
                loaded="{{ onComponentLoaded }}"
                hint="(Console output)"
                text='{{ outputValue }}'
                class="input input-border"
                backgroundColor="rgb(240,240,240)"
            />
        </ScrollView>
        <FlexboxLayout height="10%" flexDirection="row" alignItems="center" justifyContent="space-around">
            <!-- <Button text="View output" tap="{{ output }}" class="btn btn-primary btn-active"/> -->
            <Button text="Run code" tap="{{ run }}" class="btn btn-primary btn-active"/>
            <Button text="Clear" tap="{{ clear }}" class="btn btn-primary btn-active"/>
        </FlexboxLayout>
    </FlexboxLayout>
</Page>